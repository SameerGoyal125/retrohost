# Divergent Paper

A minimal synthetic "paper" that does NOT reproduce. Used as a test fixture for
the Retrohost reproduction auditor.

## Claim

**Table 1** reports the mean of each numeric column in `data/input.csv`.

The claimed result lives in `results/claimed.csv` and is **stale**: it was
generated by an older version of `analysis.py` that used a different formula
(it summed instead of averaging). Re-running the current `analysis.py` writes
its actual output to `results/table.csv`, which differs from
`results/claimed.csv`, so the claimed result no longer matches the code.

## Reproduce

```bash
python3 analysis.py
```

## Expected classification

FAILED (or PARTIAL) — re-running `analysis.py` produces `results/table.csv`
(8.5/9.5/10.5) that differs from the claimed result in `results/claimed.csv`
(51.0/57.0/63.0). This is a realistic reproducibility failure (stale claimed
artifact), not a planted constant.
